#!/bin/bash

# Uncomment and set TAG if it should be the same for every build, 
# otherwise a timestamp and "latest" will be used.
# export TAG="0.0.4"
export IMAGE_NAME=helxplatform/pgadmin4
export REGISTRY=containers.renci.org
